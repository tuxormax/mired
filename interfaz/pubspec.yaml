name: mired_interfaz
description: "Interfaz de MiRed: mapeo de redes."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.3
  # Solo para descargar el mapa exportado y abrir el enlace al codigo fuente.
  # Se usa unicamente desde servicios/descarga_web.dart, que ningun otro
  # objetivo de compilacion importa.
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
